[book]
   title = "Demos Network Documentation"
   authors = ["KyneSys Labs"]
   language = "en"
   multilingual = false
   src = "src"  # adjust if your markdown files are elsewhere
   
   [build]
   build-dir = "book"  # this matches the artifact path in the action
